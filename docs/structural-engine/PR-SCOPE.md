# PR Scope: Structural Quality Finalization

This branch finalizes the structural-packaging quality loop without claiming
unknown golden construction facts.

It removes automatic camera movement from fold state, adds a regression test
for that separation, installs a clean CI gate, records machine-readable quality
status, and preserves the fail-closed golden construction workflow.
